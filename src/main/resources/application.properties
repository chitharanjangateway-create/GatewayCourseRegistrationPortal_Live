# Server Port (Supports cloud hosts like Render/Railway which set PORT dynamically)
server.port=${PORT:${APP_PORT:8080}}

# TiDB Cloud MySQL Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/gss_course_db?createDatabaseIfNotExist=true&useSSL=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:3NehYEN5FkpJEBn.root}
spring.datasource.password=${DB_PASSWORD:77EACSHge7rg9Vwa}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Application info
spring.application.name=Gateway Software Solutions Course Registration Portal

# Logging
logging.level.com.example.course_registration_portal=INFO
